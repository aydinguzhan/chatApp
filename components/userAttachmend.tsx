import * as React from 'react';
import { View, Text } from 'react-native-animatable';

export interface IUserAttachmendProps {
}

export default function UserAttachmend(props: IUserAttachmendProps) {
    return (
        <View>
            <Text>Profil</Text>
        </View>
    );
}
